// Firefox MV3 后台脚本（event page，空占位）
// 与 Chromium 的 service worker 功能对等，当前无后台逻辑。
// 注意：MV3 中 Firefox 使用 background.scripts（event page），
// 与 Chromium 的 background.service_worker 字段不同，见 manifest.json。
