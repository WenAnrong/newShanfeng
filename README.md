# 山风新页（一个浏览器首页）

山风新页是一个 Chrome/Firefox 浏览器新标签页扩展，在用户打开新标签时替换默认空白页。

## 编译方式

```bash
npm run build
```

成功构建后，`dist` 下的内容将自动复制到插件目录（`extensions/Chromium` 和 `extensions/Firefox`）下，直接在浏览器中加载插件即可。

## 目录结构

暂定

## 设计文档

[DESIGN.md](DESIGN.md)
